name: googleads_mobile_sdk_sample
description: A Google Ads Mobile SDK sample
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  riverpod: ^0.13.0
  flutter_riverpod: ^0.13.0
  google_mobile_ads: ^0.11.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
