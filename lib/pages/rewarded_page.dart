import 'package:flutter/material.dart';

class RewardedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('rewarded page'),
    );
  }
}